import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function HistoryPage() {
  return (
    <PagePlaceholder
      title="History"
      description="Browse past predictions, actual delivery times, and model version comparisons."
    />
  );
}

import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function ModelRegistryPage() {
  return <PagePlaceholder title="Model Registry" description="Centralized model catalog, versioning, and staging controls." />;
}

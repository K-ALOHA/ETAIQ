import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function MonitoringPage() {
  return <PagePlaceholder title="Monitoring" description="Live health checks, metrics, and alerting for prediction services." />;
}
